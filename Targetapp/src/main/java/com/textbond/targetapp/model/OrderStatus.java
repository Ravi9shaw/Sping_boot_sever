package com.textbond.targetapp.model;
/*
This is a enum Class we are using this bcz
Only allowed states can exist

Compiler enforces correctness

No random values possible
 */
public enum OrderStatus {
    PENDING,
    APPROVED,
    REJECTED
}
